import { createRouter, createWebHistory } from 'vue-router';

const routes = [
  // Add route objects for each main section/component if needed
];

const router = createRouter({
  history: createWebHistory(),
  routes,
});

export default router;
